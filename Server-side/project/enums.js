
export default (function(){
    var that = {
        UNAUTHORIEZED_TOKEN : "Unable to authorize token",
        NO_TOKEN_PROVIDED : "No token provided",
        EMPTY_FIELD : "Field is missing",
        
    }
    return that;
})();